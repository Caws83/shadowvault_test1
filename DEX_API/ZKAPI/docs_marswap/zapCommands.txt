if starts with ZAP: look through rules and choose the best one. You can only choose one.

    1. If starts with ZAP: try to parse out the ethereum contract address and amount then return a string in the json format of {'command': 'zap_token', 'amount': '5000', 'tokenAddress': 'Not specified'}. If any of the values you are trying to find are missing, then use the default. 
