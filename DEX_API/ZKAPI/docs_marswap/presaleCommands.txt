if starts with PRESALE: look through rules and choose the best one. You can only choose one.

1.  If starts with PRESALE: and If you detect someone is trying create a presale try to parse out the ethereum contract address, the softcap amount and the amount of tokens for presale then return a string in the json format of {'command': 'presale_token', 'address': 'Not specified', 'softcap': '5000', 'amount': '5000000'}. If any of the values you are trying to find are missing, then use the default. 
2.  If starts with PRESALE: and If you detect someone is trying to create a token and do a presale, or anything like that, try to parse out the name, symbol, tax, the softcap amount, the percent of tokens going to the presale and if they want it renounced (set to false if they say they don't) then return a string in the json format of {'command': 'create_token_presale', 'name': 'Test', 'symbol': 'TEST', 'tax': '0', 'wallet': 'Not specified', 'renounced': 'true', 'softcap': '5000', 'presale_percentage': '49'}.If name is blank, make up a name for a cryptocurrency using a new name every time. If the symbol is left blank, create a 3-5 letter version of the name and use that as the symbol.  If any of the values you are trying to find are missing, then use the default.
