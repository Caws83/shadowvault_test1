# Netlify → Site settings → Environment variables (build + runtime for VITE_*)
#
# The browser loads these. It does NOT load Railway secrets.
# ChangeNOW + Hyperliquid keys stay on Railway only.

# REQUIRED: full URL with https:// — Netlify rejects bare hostnames in _redirects (prebuild auto-prepends https:// if missing).
# No trailing slash. Alias: RAILWAY_PUBLIC_URL
VITE_API_URL=https://your-service.up.railway.app

# Railway DEX API: set FRONTEND_ORIGINS on Railway if you use a custom domain (not *.netlify.app).

# Optional: same base if AI agent uses a dedicated path (often same as VITE_API_URL)
VITE_AI_AGENT_API_URL=https://your-service.up.railway.app
VITE_ENABLE_AI_AGENT=true

# Chain / UI (match your deployment)
VITE_CHAIN_ID=97
VITE_GTAG=GTM-TLF66T4
VITE_NODE_1=https://data-seed-prebsc-1-s1.binance.org:8545
VITE_NODE_2=https://bsc-dataseed.binance.org
VITE_NODE_3=https://eth.llamarpc.com
VITE_NODE_PRODUCTION=https://rpc.sepolia.org
VITE_GRAPH_API_PROFILE=https://api.thegraph.com/subgraphs/name/pancakeswap/profile
VITE_GRAPH_API_PREDICTION=https://api.thegraph.com/subgraphs/name/pancakeswap/prediction-v2
VITE_GRAPH_API_LOTTERY=https://api.thegraph.com/subgraphs/name/pancakeswap/lottery
VITE_SNAPSHOT_BASE_URL=https://hub.snapshot.org
VITE_SNAPSHOT_VOTING_API=https://voting-api.pancakeswap.info/api

# Do NOT put these on Netlify (they belong on Railway / DEX API only):
# CHANGENOW_API_KEY, HYPERLIQUID_AGENT_PRIVATE_KEY, OPENAI_API_KEY
