const getTokenLogoURL = (address: string) => `/images/tokens/${address.toUpperCase()}.png`

export const getTokenTokenLogoURL = (address: string) =>
  `/images/tokens/${address.toUpperCase()}.png`

export default getTokenLogoURL
