# Netlify deployment config for ShadowVault Protocol
[build]
  base = "FRONTEND/MAINDEX"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
